name: コミット要約の自動生成

on:
  push:
    branches:
      - main
      - master

jobs:
  summarize:
    uses: souri-t/own-reusable-workflows/.github/workflows/summarize.yml@main
    permissions:
      contents: write
    secrets:
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
